---
import { t, type Lang } from '../i18n/ui';
interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
const s = t(lang);
---

{lang === 'en' && (
  <p class="mb-6 rounded border border-warning/40 bg-warning/10 px-4 py-2 text-sm text-warning">
    {s.en_incomplete_notice}
  </p>
)}
